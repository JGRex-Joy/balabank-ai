# API 
В ```app/services``` есть два файла ```llm.py``` и ```server_embedder.py``` - там нужно указать ```api_key```, сгенерь сам с *Google AI studio* (у меня просто лимит закончился)

# Зависимости
```pip install -r requirements.txt"```

# Запускаешь как обычно
```uvicorn app.main:app```